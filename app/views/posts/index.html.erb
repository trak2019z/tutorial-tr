<h3> lista wpisow</h3>

<p><%= link_to "Dodaj nowy wpis", new_post_path, class: "btn btn-success" %></p>

<table class="table table-striped">
  <thead>
  <tr>
    <th>#</th>
    <th>Tytul</th>
    <th>Autor</th>
    <th>Opublikowany?</th>
    <th>Tresc</th>
    <th>Akcje</th>
  </tr>
  </thead>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.id %></td>
      <td><%= post.title %></td>
      <td><%= post.author %></td>
      <td><%= post.published ? "tak" : "nie" %></td>
      <td><%= post.body %></td>
      <td>
      <%= link_to "Pokaż", post_path(post), class: "btn btn-default btn-xs" %>
        <%= link_to "Edytuj", edit_post_path(post), class: "btn btn-default btn-xs" %>
      <%= button_to "Usun", post_path(post), method: :delete, class: "btn btn-danger btn-xs" %>



      </td>
    </tr>
  <% end %>

</table>